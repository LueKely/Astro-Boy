import { FormButton } from '../composables/button';

export function RamForm() {
    return (
        <form className="ram">
            <fieldset>
                <legend>Ram</legend>
                <FormButton name="Gaming"></FormButton>
            </fieldset>
        </form>
    );
}
